<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="developer.com.krishna.duly">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".Duly"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Teacher_main">

        </activity>
        <activity android:name=".presenter.AddNotice"/>
        <activity android:name=".presenter.AddStudent"/>
        <activity android:name=".presenter.MarkAttendance"/>
        <activity android:name=".presenter.DateClass"
            android:noHistory="true"/>
        <activity android:name=".EntryActivity"
            android:noHistory="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name=".Teacher_login"/>
        <activity android:name=".Student_login"
            android:noHistory="true"/>
        <activity android:name=".Student_main"/>
    </application>

</manifest>